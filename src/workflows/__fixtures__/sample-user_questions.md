1. Should authentication use SSO only? **blocker**
2. What is the default session timeout?
3. Can operators defer non-blocker items? **blocker**
4. Which environments need staging parity?
5. Should docs sync run on every sub-issue?
6. What is the rollback policy?
